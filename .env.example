# Resend API key for contact form (required for production)
# Get one at https://resend.com
RESEND_API_KEY=re_xxxxxxxxxxxx

# Optional: Use a verified domain for "from" address
# Default is onboarding@resend.dev (works for dev, but recipient sees Resend)
# RESEND_FROM_EMAIL=contact@signalandform.net
